[[stack]]
name = "home-assistant"
description = "Home Assistant — home automation (official homeassistant/home-assistant image, Container install)"
tags = ["home-assistant"]

[stack.config]
server = "lab53"
links = ["https://home-assistant.hectortoral.com"]
linked_repo = "hec7orci7o/svc-hub"
run_directory = "home-assistant"
file_paths = ["docker-compose.yml"]
registry_provider = "docker.io"
registry_account = "hec7or"

environment = """
TZ=Europe/Madrid
"""
