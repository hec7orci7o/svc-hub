# "lab53" is auto-created on Core's first boot (KOMODO_FIRST_SERVER_NAME in .env) — keep
# this block in sync with that so ResourceSync doesn't detect a diff.
#
# No `address`: Periphery connects outbound (dials Core), not the other way around — Komodo
# only expects a Core->Periphery address when this is set, confirmed against Komodo's own
# source (server.rs doc comment).

[[server]]
name = "lab53"
description = "Komodo Core"
tags = ["core"]

[server.config]
region = "homelab"
enabled = true

[[server]]
name = "lab54"
description = "Periphery (fleet)"
tags = ["fleet"]

[server.config]
region = "homelab"
enabled = true

[[server]]
name = "lab55"
description = "Periphery (fleet)"
tags = ["fleet"]

[server.config]
region = "homelab"
enabled = true

[[server]]
name = "lab56"
description = "Periphery (fleet)"
tags = ["fleet"]

[server.config]
region = "homelab"
enabled = true

[[server]]
name = "lab57"
description = "Periphery (fleet)"
tags = ["fleet"]

[server.config]
region = "homelab"
enabled = true

[[server]]
name = "lab58"
description = "Periphery (fleet)"
tags = ["fleet"]

[server.config]
region = "homelab"
enabled = true

# Referenced by every stack's `linked_repo` below instead of each one repeating
# git_provider/repo/branch. `repo` uses owner/svc-hub form, matching `name`.
[[repo]]
name = "hec7orci7o/svc-hub"

[repo.config]
git_account = "hec7orci7o"
repo = "hec7orci7o/svc-hub"
links = [
  "https://github.com/hec7orci7o/svc-hub"
]

[[procedure]]
name = "Backup Core Database"
description = "Triggers the Core database backup at the scheduled time."
tags = ["system"]
config.schedule = "Every day at 01:00"

[[procedure.config.stage]]
name = "Stage 1"
enabled = true
executions = [
  { execution.type = "BackupCoreDatabase", execution.params = {}, enabled = true }
]
