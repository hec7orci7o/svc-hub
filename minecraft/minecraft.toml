[[stack]]
name = "minecraft"
description = "Minecraft Java Edition server (official itzg/minecraft-server image)"
tags = ["minecraft"]

[stack.config]
server = "lab54"
linked_repo = "hec7orci7o/svc-hub"
run_directory = "minecraft"
file_paths = ["docker-compose.yml"]
registry_provider = "docker.io"
registry_account = "hec7or"

environment = """
TZ=Europe/Madrid

EULA=TRUE

TYPE=AUTO_CURSEFORGE
CF_SLUG=chill-homestead
CF_FILE_ID=8423728
CF_EXCLUDE_MODS=yacl

MEMORY=4G

MAX_PLAYERS=10
DIFFICULTY=normal
MOTD=A Minecraft Server

ENABLE_RCON=true
RCON_PASSWORD=[[MINECRAFT_RCON_PASSWORD]]

# RCON_CMDS_STARTUP is set in docker-compose.yml instead: it needs a real multi-line
# value, and this environment block's parser only supports single-line values.
"""
