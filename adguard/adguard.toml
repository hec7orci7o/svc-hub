[[stack]]
name = "adguard"
description = "AdGuard Home — network-wide DNS ad/tracker blocker (official adguard/adguardhome image)"
tags = ["adguard", "dns"]

[stack.config]
server = "lab53"
links = ["https://adguard.hectortoral.com"]
linked_repo = "hec7orci7o/svc-hub"
run_directory = "adguard"
file_paths = ["docker-compose.yml"]
registry_provider = "docker.io"
registry_account = "hec7or"
