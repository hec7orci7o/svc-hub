# Env (.env already covers komodo/.env, n8n/.env, cloudflared/.env — the
# pattern matches any subfolder)
.env
*.env.local

# Runtime data (bind mounts created on the host at deploy time)
n8n/local-files/
odoo/addons/
trek/data/
trek/uploads/
scanopy/data/
opengym/data/
opengym/media/

# Scratch (may contain real secrets pasted during migration/debugging — never commit)
.scratch/

# OS
.DS_Store

# Editors
.vscode/
.idea/
