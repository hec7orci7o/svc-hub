# Env (.env already covers komodo/core/.env, n8n/.env, cloudflared/.env — the
# pattern matches any subfolder)
.env
*.env.local

# Runtime data (bind mounts created on the host at deploy time)
n8n/local-files/
odoo/addons/
odoo/config/
trek/data/
trek/uploads/
scanopy/data/

# OS
.DS_Store

# Editors
.vscode/
.idea/
